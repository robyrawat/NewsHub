<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut android:shortcutId="new_article" android:enabled="true" android:icon="@mipmap/ic_launcher"
        android:shortcutShortLabel="@string/shortcut_new_article_short_label"
        android:shortcutLongLabel="@string/shortcut_new_article_long_label"
        android:shortcutDisabledMessage="@string/shortcut_disabled_message">
        <intent android:action="android.intent.action.VIEW" android:targetPackage="com.example.composedemo"
            android:targetClass="com.example.composedemo.MainActivity">
            <!-- Add extra data for your intent if needed -->
            <extra android:name="shortcut_action" android:value="new_article" />
        </intent>
        <!-- If you want to allow this shortcut to be pushed to Google Assistant,
                 include the capability linking. -->
        <capability-binding android:key="actions.intent.CREATE_MESSAGE" />
    </shortcut>
    <!-- Add more shortcuts here if needed -->
</shortcuts>
    